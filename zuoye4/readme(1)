新闻文本二分类项目（20 Newsgroups）—— GRU vs BERT 对比实践
项目简介
本项目使用 20 Newsgroups 数据集中的两个类别（alt.atheism 和 soc.religion.christian），分别采用 单层双向 GRU 和 BERT-base 模型完成二分类任务，并对两者效果进行对比分析。最终 GRU 测试准确率达 93.17%，BERT 测试准确率为 88.15%（在给定预处理和 CPU 训练条件下）。项目旨在展示不同架构在小规模文本分类任务上的性能差异，并探讨预处理策略对模型的影响。

数据集与预处理
数据来源：sklearn.datasets.fetch_20newsgroups，仅加载两个类别。

预处理步骤（两个模型共用）：

转小写

去除 HTML 标签

仅保留字母字符

去除英文停用词（ENGLISH_STOP_WORDS）

过滤长度 ≤2 的单词

构建词汇表（min_freq=2，词汇表大小 14,613）

数据划分：

原始训练集再次拆分：训练集 80%（863 条），验证集 20%（216 条）

官方测试集作为 测试集（717 条）

随机种子固定为 42，确保对比公平。

模型架构与超参数
1. 单层双向 GRU（train.py）
嵌入层：128 维，padding_idx=0

GRU 层：单层，双向，隐藏单元数 256，batch_first=True

全连接层：输入 512（双向拼接），输出 2

Dropout：嵌入层后 0.2

优化器：Adam，学习率 5e-4

训练轮次：30（早停，耐心 5）

批次大小：32

梯度裁剪：max_norm=1.0

2. BERT-base（bert_train.py）
预训练模型：bert-base-uncased

序列最大长度：128（为了在 CPU 上加速训练而截断）

优化器：AdamW，学习率 2e-5

学习率调度：线性预热（10% warmup）

训练轮次：3（早停，耐心 2）

批次大小：16

梯度裁剪：max_norm=1.0

训练设备：CPU（因硬件限制）

训练与结果
实际运行日志摘要
模型	最佳验证准确率	测试准确率	训练耗时（3轮）
GRU	94.91%	93.17%	~20 秒
BERT	92.59%	88.15%	~20 分 44 秒（CPU）
详细训练日志：

GRU：在第 12 轮达到最佳验证准确率 94.91%，测试 93.17%。

BERT：每轮验证准确率分别为 75.46%、88.89%、92.59%，测试 88.15%。

对比分析与结论
准确率：在给定预处理（去除停用词、截断至128）下，GRU 优于 BERT。可能原因：

停用词去除破坏了 BERT 所需的完整语法结构，而 GRU 更依赖关键词序列。

序列截断（128）丢失了部分上下文，削弱了 BERT 的预训练优势。

BERT 参数巨大（1.1亿），而训练样本仅 863 条，易过拟合；GRU 参数量小（约120万），更契合小数据集。

训练效率：GRU 极快，适合快速迭代；BERT 在 CPU 上耗时较长，若使用 GPU 可大幅缩短，但仍是 GRU 的数倍。

结论：在本项目的预处理和计算资源条件下，GRU 是更优选择。但若调整预处理（保留停用词、增大 max_len）并使用 GPU，BERT 很可能反超。

项目文件结构
text
.
├── news_data.py          # 数据加载、预处理、划分及 Dataset 类（共用）
├── train.py              # GRU 模型训练脚本
├── bert_train.py         # BERT 模型训练脚本
├── best_gru_model.pth    # 训练后保存的最佳 GRU 模型权重
├── best_bert_model.pth   # 训练后保存的最佳 BERT 模型权重
├── comparison.md         # 详细对比分析报告
└── README.md             # 项目说明（本文件）
运行指南
环境要求
Python 3.8+

PyTorch 1.10+

Transformers 4.20+

scikit-learn

numpy

安装依赖：

bash
pip install torch transformers scikit-learn numpy
训练与测试
GRU 模型：

bash
python train.py
将自动训练并输出验证集和测试集准确率，保存最佳模型。

BERT 模型：

bash
python bert_train.py
训练过程中每 10 个 batch 打印一次进度，结束后输出测试准确率。

查看对比：
打开 comparison.md 查看详细分析。

实验结论
本项目通过实际训练验证了在强预处理的小型文本分类任务中，轻量级 GRU 能够取得优于 BERT 的效果，同时具备极高的训练效率。这为资源受限或需要快速部署的场景提供了有益参考。