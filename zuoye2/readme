新闻文本二分类项目（20 Newsgroups）
项目简介
本项目使用 20 Newsgroups 数据集中的两个类别（`alt.atheism` 和 `soc.religion.christian`），构建一个基于 **单层双向 GRU** 的文本分类模型，完成二分类任务。最终在测试集上达到 **93.17%** 的准确率，远超预设的 70% 目标。

## 数据预处理与划分
- **数据来源**：`sklearn.datasets.fetch_20newsgroups`，仅加载 `alt.atheism` 和 `soc.religion.christian` 两个类别。
- **预处理步骤**：
  - 转小写
  - 去除 HTML 标签
  - 仅保留字母字符
  - 去除英文停用词（`ENGLISH_STOP_WORDS`）
  - 过滤长度 ≤2 的单词
  - 构建词汇表（`min_freq=2`，词汇表大小 14,613）
- **数据集划分**：
  - 原始训练集再次拆分：**训练集 80%**（863 条），**验证集 20%**（216 条）
  - 官方测试集作为 **测试集**（717 条）

---

## 模型结构（单层双向 GRU）
- **嵌入层**：将单词索引映射为 128 维稠密向量（`embed_dim=128`），`padding_idx=0`。
- **GRU 层**：单层双向 GRU（`num_layers=1, bidirectional=True`），隐藏单元数 `hidden_dim=256`。
  - 前向和后向的最后一个时间步隐藏状态被拼接，得到 512 维特征向量。
- **全连接层**：将 512 维特征映射到 2 个类别。
- **Dropout**：在嵌入层后添加 Dropout（`dropout=0.2`），防止过拟合。

> 注：虽然使用双向结构，但层数仍为 1，符合“1 层 GRU”的要求。

---

## 超参数选择
| 超参数       | 取值         | 说明                                 |
|--------------|--------------|--------------------------------------|
| 嵌入维度     | 128          | 平衡语义表达与计算资源               |
| GRU 隐藏单元 | 256          | 增加模型容量以拟合训练数据           |
| Dropout      | 0.2          | 降低正则化强度，缓解欠拟合           |
| 学习率       | 5e-4         | 比默认值稍低，更精细地更新参数       |
| 批次大小     | 32           | 适合大多数 GPU 显存，梯度估计稳定    |
| 训练轮次     | 30（早停）   | 设置足够轮次，早停耐心为 5           |

---

## 优化方法与训练策略
- **优化器**：Adam（`optim.Adam`），初始学习率 `5e-4`。
- **损失函数**：交叉熵损失（`CrossEntropyLoss`）。
- **梯度裁剪**：`max_norm=1.0`，防止梯度爆炸。
- **早停机制**：监控验证集准确率，若连续 5 个 epoch 无提升则停止训练，并保存最佳模型权重。
- **模型保存**：最佳模型保存为 `best_gru_model.pth`。

---

## 实验结果
- **最佳验证准确率**：94.91%
- **测试准确率**：**93.17%**

训练日志显示模型在第 12 个 epoch 达到最佳验证准确率，之后未再显著提升，早停在第 17 个 epoch 触发。

---

## 运行说明
1. 确保已安装依赖：`torch`, `scikit-learn`, `numpy` 等。
2. 直接运行 `train.py`，将自动加载数据、预处理、训练并输出测试结果。
3. 训练过程中会打印损失和准确率，最终输出验证集和测试集准确率。

---

## 文件说明
- `news_data.py`：数据加载、预处理、词汇表构建、数据集划分及 `Dataset`/`collate_fn` 实现。
- `train.py`：模型定义、训练循环、验证、早停、评估及保存最佳模型。
- `best_gru_model.pth`：训练得到的最佳模型权重（训练后生成）。
- `readme.md`：项目说明文档。

---

## 总结
本项目采用单层双向 GRU 配合适当的超参数和优化策略，在 20 Newsgroups 二分类任务上取得了优异的性能（测试准确率 93.17%），验证了 GRU 在短文本分类中的有效性，且模型结构简洁、训练快速。